#!/usr/bin/env bash
# -*- mode: sh; -*-

export YOFF=27px
# export HEIGHT=10%
export WIDTH=520px
export COL=3
export LINES=1
rofi -location 3 -hover-select -me-select-entry '3' -me-accept-entry MousePrimary\
    -show notif -modi "notif:$HOME/.bin/modi/rofi-volume.sh"\
    -theme-str 'inputbar {enabled: false;}'
